

SET(Document_SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR})
SET(Document_BINARY_DIR ${CMAKE_CURRENT_BINARY_DIR})

FILE(GLOB EPS_FILES "*.eps")
   FOREACH(EPS_FILE ${EPS_FILES})
      CONFIGURE_FILE(${EPS_FILE} "${CMAKE_CURRENT_BINARY_DIR}"/${EPS_FILE} COPYONLY)
   ENDFOREACH(EPS_FILE)

CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/dirtree.sty ${CMAKE_CURRENT_BINARY_DIR} COPYONLY)
CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/unstr.bst ${CMAKE_CURRENT_BINARY_DIR} COPYONLY)
CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/dirtree.tex ${CMAKE_CURRENT_BINARY_DIR} COPYONLY)

INCLUDE(UseLATEX.cmake)

ADD_LATEX_DOCUMENT(programmer_manual.tex
CONFIGURE programmer_manual.tex
#INPUTS front.tex introduction.tex
BIBFILES ./nifty_rec.bib
IMAGE_DIRS ./
USE_INDEX
#DEFAULT_PDF
)

INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/programmer_manual.pdf DESTINATION ${DOC_INSTALL_DIR})
